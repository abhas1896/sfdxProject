<apex:page controller="accRecords">
    <apex:pageBlock title="My Accounts">
        <apex:pageBlockTable value="{!accountList}" var="item">
          <apex:column value="{!item.Name}"/>
          <apex:column value="{!item.Id}"/>        
          <apex:column value="{!item.CreatedDate}"/>
        </apex:pageBlockTable>
    
    
    
    </apex:pageBlock>
</apex:page>